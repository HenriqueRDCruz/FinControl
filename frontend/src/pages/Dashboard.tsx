import {useEffect,useState} from 'react'; import {api} from '../services/api'; import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer,BarChart,Bar,PieChart,Pie,Cell,Legend} from 'recharts';
const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
export default function Dashboard(){const [d,setD]=useState<any>(null); useEffect(()=>{api.get('/dashboard').then(r=>setD(r.data))},[]); if(!d)return <p>Carregando dashboard...</p>; return <><div className="page-title"><div><h2>Dashboard</h2><p>Visão geral das suas finanças.</p></div></div><div className="grid">{[['Receitas',d.totais.receitas,'positive'],['Despesas',d.totais.despesas,'negative'],['Saldo',d.totais.saldo,d.totais.saldo>=0?'positive':'negative'],['Investido',d.totais.investido,'']].map(x=><div className="card" key={x[0]}><div className="label">{x[0]}</div><div className={'value '+x[2]}>{money(x[1])}</div></div>)}</div><div className="charts"><div className="card"><h3>Evolução mensal</h3><ResponsiveContainer width="100%" height={300}><LineChart data={d.evolucao}><XAxis dataKey="mes"/><YAxis/><Tooltip formatter={(v:any)=>money(Number(v))}/><Legend/><Line type="monotone" dataKey="receitas" name="Receitas"/><Line type="monotone" dataKey="despesas" name="Despesas"/></LineChart></ResponsiveContainer></div><div className="card"><h3>Despesas por categoria</h3><ResponsiveContainer width="100%" height={300}><PieChart><Pie data={d.categorias} dataKey="valor" nameKey="nome" outerRadius={95} label>{d.categorias.map((_:any,i:number)=><Cell key={i}/>)}</Pie><Tooltip formatter={(v:any)=>money(Number(v))}/><Legend/></PieChart></ResponsiveContainer></div></div><div className="card" style={{marginTop:16}}><h3>Comparativo</h3><ResponsiveContainer width="100%" height={280}><BarChart data={d.evolucao}><XAxis dataKey="mes"/><YAxis/><Tooltip formatter={(v:any)=>money(Number(v))}/><Legend/><Bar dataKey="receitas" name="Receitas"/><Bar dataKey="despesas" name="Despesas"/></BarChart></ResponsiveContainer></div></>}
